import React from 'react';
import { Container, Text } from '@components/common';

export const AvailableScreen = () => {
  return (
    <Container safe>
      <Text>Available</Text>
    </Container>
  );
};
