import React from 'react';
import { Container, Text } from '@components/common';

export const CalendarScreen = () => {
  return (
    <Container safe>
      <Text>Calendar</Text>
    </Container>
  );
};
