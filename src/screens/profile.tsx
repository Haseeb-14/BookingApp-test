import React from 'react';
import { Container, Text } from '@components/common';

export const ProfileScreen = () => {
  return (
    <Container safe>
      <Text>Profile</Text>
    </Container>
  );
};
