import React from 'react';
import { Container, Text } from '@components/common';

export const ScheduleScreen = () => {
  return (
    <Container safe>
      <Text>Schedule</Text>
    </Container>
  );
};
